<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Pour toi, Odile 💖</title>

  <style>
    :root{
      --bg1:#05050a; --bg2:#0b0b18; --txt:#f4f4ff; --muted:#c7c7ff;
      --p1:#ff4fd8; --p2:#7c5cff; --p3:#35d6ff;
    }
    *{box-sizing:border-box}
    body{
      margin:0; min-height:100vh; display:grid; place-items:center;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color:var(--txt);
      background:
        radial-gradient(1200px 800px at 20% 20%, rgba(255,79,216,.18), transparent 60%),
        radial-gradient(900px 600px at 80% 30%, rgba(124,92,255,.18), transparent 55%),
        radial-gradient(1000px 700px at 40% 90%, rgba(53,214,255,.12), transparent 60%),
        linear-gradient(180deg, var(--bg1), var(--bg2));
      overflow:hidden;
    }

    /* particules */
    .spark{
      position:absolute; inset:-20vh -20vw; pointer-events:none; opacity:.65;
      background-image:
        radial-gradient(circle at 10% 20%, rgba(255,79,216,.9) 0 1px, transparent 2px),
        radial-gradient(circle at 70% 30%, rgba(124,92,255,.9) 0 1px, transparent 2px),
        radial-gradient(circle at 40% 80%, rgba(53,214,255,.9) 0 1px, transparent 2px),
        radial-gradient(circle at 90% 70%, rgba(255,255,255,.7) 0 1px, transparent 2px);
      background-size: 240px 240px, 260px 260px, 300px 300px, 220px 220px;
      animation: drift 14s linear infinite;
      filter: blur(.2px);
    }
    @keyframes drift{
      from{ transform: translate3d(0,0,0) scale(1); }
      to{ transform: translate3d(-6vw, 8vh, 0) scale(1.05); }
    }

    .card{
      width:min(920px, 92vw);
      border-radius:28px;
      padding:28px 22px 18px;
      background: linear-gradient(135deg, rgba(255,255,255,.10), rgba(255,255,255,.03));
      border:1px solid rgba(255,255,255,.12);
      box-shadow: 0 20px 60px rgba(0,0,0,.55), 0 0 120px rgba(255,79,216,.08);
      backdrop-filter: blur(10px);
      position:relative;
    }

    .halo{
      position:absolute; inset:-2px;
      border-radius:30px;
      background: conic-gradient(from 180deg,
        rgba(255,79,216,.35),
        rgba(124,92,255,.35),
        rgba(53,214,255,.25),
        rgba(255,79,216,.35)
      );
      filter: blur(18px);
      opacity:.25;
      z-index:-1;
      animation: spin 6s linear infinite;
    }
    @keyframes spin{ to{ transform: rotate(1turn);} }

    .title{
      font-size: clamp(28px, 4vw, 46px);
      margin: 0 0 6px 0;
      letter-spacing:.2px;
      text-shadow: 0 0 28px rgba(255,79,216,.25);
    }
    .sub{
      margin:0 0 18px 0;
      color:var(--muted);
      opacity:.95;
    }

    .line{
      font-size: clamp(16px, 2.2vw, 20px);
      line-height: 1.7;
      margin: 0;
      opacity:0;
      transform: translateY(10px);
      animation: rise .9s ease forwards;
    }
    .line:nth-of-type(1){ animation-delay: .10s;}
    .line:nth-of-type(2){ animation-delay: .35s;}
    .line:nth-of-type(3){ animation-delay: .60s;}
    .line:nth-of-type(4){ animation-delay: .85s;}
    .line:nth-of-type(5){ animation-delay: 1.10s;}
    .line:nth-of-type(6){ animation-delay: 1.35s;}
    @keyframes rise{ to{ opacity:1; transform: translateY(0); } }

    .divider{
      height:1px;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,.22), transparent);
      margin:18px 0;
    }

    .actions{ display:flex; flex-wrap:wrap; gap:12px; align-items:center; }

    .btn{
      display:inline-flex; align-items:center; justify-content:center; gap:10px;
      padding:14px 16px;
      border-radius:16px;
      color:#0a0814;
      background: linear-gradient(135deg, var(--p1), var(--p2));
      border:0;
      font-weight:900;
      text-decoration:none;
      box-shadow: 0 12px 30px rgba(124,92,255,.22), 0 0 40px rgba(255,79,216,.18);
      transform: translateY(0);
      transition: transform .15s ease, filter .15s ease;
      cursor:pointer;
    }
    .btn:active{ transform: translateY(1px); }
    .btn:hover{ filter: brightness(1.05); }

    .btn.secondary{
      background: linear-gradient(135deg, rgba(255,255,255,.18), rgba(255,255,255,.08));
      color: var(--txt);
      border:1px solid rgba(255,255,255,.14);
      box-shadow:none;
      font-weight:800;
    }

    .note{
      margin-top:10px;
      color: rgba(255,255,255,.75);
      font-size: 13px;
    }

    .footer{
      margin-top:16px;
      padding-top:14px;
      border-top:1px solid rgba(255,255,255,.10);
      color: rgba(255,255,255,.78);
      font-weight:700;
      text-align:center;
    }

    /* Modal lecteur */
    .modal{
      position:fixed; inset:0; display:none; place-items:center;
      background: rgba(0,0,0,.65);
      padding:18px;
      z-index:50;
    }
    .modal.open{ display:grid; }
    .modalBox{
      width:min(920px, 96vw);
      border-radius:22px;
      background: rgba(10,10,20,.92);
      border:1px solid rgba(255,255,255,.14);
      box-shadow: 0 20px 80px rgba(0,0,0,.7);
      overflow:hidden;
    }
    .modalHead{
      display:flex; align-items:center; justify-content:space-between;
      padding:12px 14px;
      border-bottom:1px solid rgba(255,255,255,.10);
    }
    .modalTitle{ font-weight:900; letter-spacing:.2px; }
    .closeBtn{
      border:0; background:transparent; color:rgba(255,255,255,.85);
      font-size:18px; cursor:pointer; padding:6px 10px; border-radius:12px;
    }
    .closeBtn:hover{ background: rgba(255,255,255,.08); }
    .videoWrap{
      aspect-ratio: 16 / 9;
      width:100%;
      background:#000;
    }
    iframe{ width:100%; height:100%; border:0; display:block; }

    .modalNote{
      padding:10px 14px 14px;
      color: rgba(255,255,255,.75);
      font-size: 13px;
    }
  </style>
</head>

<body>
  <div class="spark"></div>

  <main class="card">
    <div class="halo"></div>

    <h1 class="title">Pour toi, Odile 💖</h1>
    <p class="sub">Joyeux anniversaire 🎂✨</p>

    <div>
      <p class="line">Aujourd'hui, le monde a un an de plus à profiter de toi.</p>
      <p class="line">Et moi, je prends un moment pour te dire quelque chose d'important.</p>
      <p class="line">Tu es cette présence qui apaise, ce sourire qui change l'ambiance, cette personne qui compte plus que les mots ne peuvent l'expliquer.</p>
      <p class="line">Être à tes côtés, c'est simple… mais précieux.</p>
      <p class="line">En ce jour spécial, je te souhaite tout ce que ton cœur mérite :</p>
      <p class="line">de la joie, de la douceur, des rêves qui prennent forme, et beaucoup d'amour — sincère, calme, vrai.</p>
    </div>

    <div class="divider"></div>

    <div class="actions">
      <!-- Bouton WOW : ouvre le lecteur dans la page -->
      <button class="btn" id="playBtn">▶️ Lancer la chanson</button>

      <!-- Bouton sûr à 100% : ouvre YouTube direct -->
      <a class="btn secondary" id="ytLink" href="https://www.youtube.com/watch?v=tVKaN_H35xs" target="_blank" rel="noopener">
        Ouvrir sur YouTube ↗
      </a>
    </div>

    <div class="note">
      iPhone bloque souvent l'autoplay avec son : appuie sur “Lancer la chanson”, et ça démarre ✅
    </div>

    <div class="footer">Fait avec amour — Brian ❤️</div>
  </main>

  <!-- Modal -->
  <section class="modal" id="modal">
    <div class="modalBox">
      <div class="modalHead">
        <div class="modalTitle">Pour Odile 🎶</div>
        <button class="closeBtn" id="closeBtn">✕</button>
      </div>

      <div class="videoWrap" id="videoWrap">
        <!-- l’iframe sera injectée ici -->
      </div>

      <div class="modalNote">
        Si le son ne part pas, touche la vidéo une fois (restriction iOS). 💖
      </div>
    </div>
  </section>

  <script>
    const VIDEO_ID = "tVKaN_H35xs";

    const playBtn = document.getElementById("playBtn");
    const modal = document.getElementById("modal");
    const closeBtn = document.getElementById("closeBtn");
    const videoWrap = document.getElementById("videoWrap");

    function openModal(){
      modal.classList.add("open");

      // Autoplay après clic (user gesture) -> beaucoup plus fiable
      // mute=0 mais iOS peut forcer une interaction, d’où le bouton YouTube en backup
      videoWrap.innerHTML = `
        <iframe
          src="https://www.youtube.com/embed/${VIDEO_ID}?autoplay=1&playsinline=1&rel=0"
          title="YouTube player"
          allow="autoplay; encrypted-media; picture-in-picture"
          allowfullscreen>
        </iframe>
      `;
    }

    function closeModal(){
      modal.classList.remove("open");
      videoWrap.innerHTML = ""; // stop la musique en fermant
    }

    playBtn.addEventListener("click", openModal);
    closeBtn.addEventListener("click", closeModal);
    modal.addEventListener("click", (e) => { if(e.target === modal) closeModal(); });
  </script>
</body>
</html>
